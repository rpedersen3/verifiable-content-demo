{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "./dist",
    "types": ["@cloudflare/workers-types", "node"],
    // Consume built packages — see the workspace tsconfig for rationale.
    "paths": {}
  },
  "include": ["src/**/*"]
}
